import styled from "styled-components"

const Footer = () => {
  return <FooterComponent>푸터입니다.</FooterComponent>
}

export default Footer

const FooterComponent = styled.footer`
  background: #ddd;
  height: 150px;
`
